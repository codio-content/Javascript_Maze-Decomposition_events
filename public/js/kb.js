
function keyPressed() {

  // reload the page
  if ( keyPressed == 'R' ) {
    document.location.reload(true);
  }

  if ( keyPressed == 'LEFT' ) {
    player.moveleft()
  } 
  if ( keyPressed == 'LEFT' ) {
    player.moveleft()
  } 

  
};

