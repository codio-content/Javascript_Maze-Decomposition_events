<!DOCTYPE html>
<html>
  <head>
    <title>One</title>

    <% include include/game.ejs %>      
    
    <script>

      document.addEventListener('DOMContentLoaded', function() {
        createRandomMaze()
        startFromContainer('game')

        // Load the user script file
        loadUserScript('/content/js/collision-events.js')        
        
      })

      // Once user script is loaded, do whatever else needs to be done
      function userScriptLoaded() {
        setPlayerHitEnergyEventHandler(hitEnergyEvent);
        //setPlayerHitObjectEventHandler(hitWallEvent);
        setPlayerHitMonsterEventHandler(hitMonsterEvent);
      }
            
    </script>
    
  </head>
  <body>
    <div id="game"></div>
  </body>
</html>
